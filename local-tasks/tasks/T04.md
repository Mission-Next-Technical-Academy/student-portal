# T04 - Security Copilot plugin inventory

You are generating fictional demo data for a local SC-200 training lab.

RULES (violating any = task failure):
- Create exactly ONE file: local-tasks/out/t04-copilot-plugins.js
- Do NOT read or edit any other file. Do NOT run git or install anything.
- All data is FICTIONAL demo data for a local training lab.
- No real URLs: the characters "http" must not appear anywhere in the file.
- No real companies/people. Fictional analyst names like 'R. Vance', 'M. Okafor'; fictional org names like 'Northwind Ops', 'Fabrikam-style is NOT allowed either — invent your own'.
- Any hash/ID-like strings use repeated-letter style: 'aaaa1111bbbb'.
- The file must be plain JavaScript and pass node --check.
- End the file with exactly:
if (typeof module !== 'undefined') { module.exports = { COPILOT_PLUGINS }; }

FILE CONTENT SPEC:
- Declare: const COPILOT_PLUGINS = [ ... ]; with EXACTLY 12 objects.
- Keys: id ('pl-01'..'pl-12'), name, category ('First-party' for 7, 'Non-Microsoft' for 2, 'Custom' for 3), status ('On' or 'Off'), description (one sentence), setupNote (one sentence, e.g. which workspace or parameter it needs, fictional).
- First-party names to use: 'Microsoft Defender XDR','Microsoft Sentinel','Microsoft Entra','Microsoft Intune','Microsoft Defender Threat Intelligence','Microsoft Purview','Azure Firewall'.
- Non-Microsoft: invent 2 fictional vendor plugins (e.g. 'NetScope CASB').
- Custom: one KQL plugin, one API plugin, one prompt-only plugin, fictional.

Write the file now, then stop.
