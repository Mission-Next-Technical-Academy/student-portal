# T03 - Security Copilot promptbook library

You are generating fictional demo data for a local SC-200 training lab.

RULES (violating any = task failure):
- Create exactly ONE file: local-tasks/out/t03-copilot-promptbooks.js
- Do NOT read or edit any other file. Do NOT run git or install anything.
- All data is FICTIONAL demo data for a local training lab.
- No real URLs: the characters "http" must not appear anywhere in the file.
- No real companies/people. Fictional analyst names like 'R. Vance', 'M. Okafor'; fictional org names like 'Northwind Ops', 'Fabrikam-style is NOT allowed either — invent your own'.
- Any hash/ID-like strings use repeated-letter style: 'aaaa1111bbbb'.
- The file must be plain JavaScript and pass node --check.
- End the file with exactly:
if (typeof module !== 'undefined') { module.exports = { COPILOT_PROMPTBOOKS }; }

FILE CONTENT SPEC:
- Declare: const COPILOT_PROMPTBOOKS = [ ... ]; with EXACTLY 8 objects.
- Keys: id ('pb-01'..'pb-08'), name, source ('Microsoft' for 5, 'Custom' for 3), description (one sentence), inputs (array of 0-2 strings like 'Incident ID','Device name'), prompts (array of 3-5 strings, each an ordered prompt in the sequence).
- Themes to cover: incident investigation, suspicious script analysis, threat actor profile, vulnerability impact assessment, user compromise assessment, email threat triage, plus 3 custom ones a SOC would write (e.g. shift handoff summary).
- Example: { id:'pb-01', name:'Incident investigation', source:'Microsoft', description:'Step-by-step triage of an incident.', inputs:['Incident ID'], prompts:['Summarize incident <ID>','List impacted entities','List related alerts','Suggest response actions','Draft an executive summary'] },

Write the file now, then stop.
