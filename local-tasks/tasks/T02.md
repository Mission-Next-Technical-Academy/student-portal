# T02 - Security Copilot session transcripts

You are generating fictional demo data for a local SC-200 training lab.

RULES (violating any = task failure):
- Create exactly ONE file: local-tasks/out/t02-copilot-transcripts.js
- Do NOT read or edit any other file. Do NOT run git or install anything.
- All data is FICTIONAL demo data for a local training lab.
- No real URLs: the characters "http" must not appear anywhere in the file.
- No real companies/people. Fictional analyst names like 'R. Vance', 'M. Okafor'; fictional org names like 'Northwind Ops', 'Fabrikam-style is NOT allowed either — invent your own'.
- Any hash/ID-like strings use repeated-letter style: 'aaaa1111bbbb'.
- The file must be plain JavaScript and pass node --check.
- End the file with exactly:
if (typeof module !== 'undefined') { module.exports = { COPILOT_TRANSCRIPTS }; }

FILE CONTENT SPEC:
- Declare: const COPILOT_TRANSCRIPTS = [ ... ]; with EXACTLY 2 objects.
- Keys per object: sessionId ('cs-001' and 'cs-003'), steps (array of 5 objects).
- Keys per step: role ('analyst' or 'copilot'), text (1-3 sentences), plugin (one of 'Defender XDR','Sentinel','MDTI','none'), pinned (true/false).
- Transcript 1: analyst investigates a phishing incident: ask for incident summary -> copilot summarizes -> ask which users clicked -> copilot lists 2 fictional users -> ask for recommended actions.
- Transcript 2: analyst asks about a suspicious PowerShell command: paste command 'powershell -enc aaaa1111' -> copilot explains it decodes a downloader -> ask for related MDTI intel -> copilot cites a fictional actor 'Nickel Sleet' -> ask draft summary.
- Alternate roles analyst/copilot. Copilot answers must sound helpful but generic; NO real threat intel.

Write the file now, then stop.
