# T11 - Security Copilot plugins VIEW (pilot)

You are writing ONE page view for a local SC-200 training lab portal.

FIRST read this short file for the exact contract (it is only ~90 lines):
/home/alex/defender-lab/ui/NODE_MAP.md

RULES (violating any = task failure):
- Create exactly ONE file at this ABSOLUTE path (use it verbatim):
  /home/alex/defender-lab/local-tasks/out/views/v11-copilot-plugins.js
- Do NOT read or edit any other file besides NODE_MAP.md. Do NOT run git.
- The characters "http" must not appear anywhere. All content fictional.
- Do NOT use onMount, document, window, fetch, or localStorage.
- Only use helper functions esc(), cap(), fmtTime(), toast() — nothing else.
- Only use CSS classes listed in NODE_MAP.md.

FILE CONTENT SPEC:
- Line 1 must be exactly:
// nav: Manage | Plugins | 🧩
- Then exactly one statement registering the view:
VIEWS['copilot/plugins'] = () => `
  ...
`;
- The page uses the existing fixture const COPILOT_PLUGINS (in scope
  globally). It is an array of 12 objects with keys: id, name, category
  ('First-party' | 'Non-Microsoft' | 'Custom'), status ('On' | 'Off'),
  description, setupNote.
- Page structure:
  1. page-header block: breadcrumb "Security Copilot › <strong>Plugins</strong>",
     h1 "Plugins", a one-sentence page-subtitle, and one btn-primary button
     whose onclick is toast('Plugin catalog is a fictional lab surface.')
     labeled "Add plugin".
  2. A kpi row (class "grid"): three kpi tiles showing counts - total
     plugins (12), how many are On, how many are Custom. Compute them with
     COPILOT_PLUGINS.filter(...).length inside the template.
  3. A <table class="grid"> listing ALL plugins via
     ${COPILOT_PLUGINS.map(p => `...`).join('')} with columns:
     Name, Category (as <span class="tag">), Status (tag green when On,
     tag orange when Off), Description, Setup note. Wrap every fixture
     value in esc().
  4. A closing "card card-body" study note (3-4 own-words sentences): what
     Copilot plugins do, that first-party plugins ground answers in tenant
     security data, and that custom KQL/API plugins extend it; mention the
     precedence idea that the most specific enabled plugin answers first.
- The whole file is plain JS and must pass node --check.

Write the file now, then stop.
