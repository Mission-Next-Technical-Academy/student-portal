# T07 - Multi-cloud connector fixtures (AWS/GCP)

You are generating fictional demo data for a local SC-200 training lab.

RULES (violating any = task failure):
- Create exactly ONE file: local-tasks/out/t07-multicloud.js
- Do NOT read or edit any other file. Do NOT run git or install anything.
- All data is FICTIONAL demo data for a local training lab.
- No real URLs: the characters "http" must not appear anywhere in the file.
- No real companies/people. Fictional analyst names like 'R. Vance', 'M. Okafor'; fictional org names like 'Northwind Ops', 'Fabrikam-style is NOT allowed either — invent your own'.
- Any hash/ID-like strings use repeated-letter style: 'aaaa1111bbbb'.
- The file must be plain JavaScript and pass node --check.
- End the file with exactly:
if (typeof module !== 'undefined') { module.exports = { MC_CONNECTORS, MC_RESOURCES, MC_ALERTS }; }

FILE CONTENT SPEC:
- Declare THREE consts:
- MC_CONNECTORS: EXACTLY 2 (one AWS, one GCP). Keys: id, cloud ('AWS','GCP'), accountId (fake like '111122223333' / 'proj-aaaa1111'), plans (array from 'CSPM','Servers','Containers','Databases'), health ('Healthy','Warning'), lastSync (ISO datetime June 2026).
- MC_RESOURCES: EXACTLY 12 (mix of both clouds). Keys: id, cloud, type (e.g. 'VM instance','Storage bucket','SQL database','Container cluster'), name (fictional like 'nw-ops-vm-7'), region (e.g. 'eu-west-1','europe-west3'), riskLevel ('High','Medium','Low','None').
- MC_ALERTS: EXACTLY 2. Keys: id, cloud, title (e.g. 'Unusual role assumption on storage service'), severity, resource (a MC_RESOURCES name), description (2 sentences, fictional).
- module.exports: { MC_CONNECTORS, MC_RESOURCES, MC_ALERTS }

Write the file now, then stop.
