# T08 - Purview Audit Premium fixtures

You are generating fictional demo data for a local SC-200 training lab.

RULES (violating any = task failure):
- Create exactly ONE file at this ABSOLUTE path (use it verbatim in your write-file tool call): /home/alex/defender-lab/local-tasks/out/t08-audit-premium.js
- Do NOT read or edit any other file. Do NOT run git or install anything.
- All data is FICTIONAL demo data for a local training lab.
- No real URLs: the characters "http" must not appear anywhere in the file.
- No real companies/people. Fictional analyst names like 'R. Vance', 'M. Okafor'; fictional org names like 'Northwind Ops', 'Fabrikam-style is NOT allowed either — invent your own'.
- Any hash/ID-like strings use repeated-letter style: 'aaaa1111bbbb'.
- The file must be plain JavaScript and pass node --check.
- End the file with exactly:
if (typeof module !== 'undefined') { module.exports = { AUDIT_RETENTION_POLICIES, AUDIT_COPILOT_EVENTS }; }

FILE CONTENT SPEC:
- Declare TWO consts. BOTH must be JavaScript ARRAY literals: const X = [ {...}, {...} ]; — an object keyed by id is WRONG and fails verification.
- AUDIT_RETENTION_POLICIES: EXACTLY 5. Keys: id ('arp-1'..), name, users ('All' or 1-2 fictional user emails at fictional domain 'northwindops.example'), recordTypes (array like ['ExchangeItem','SharePointFileOperation','CopilotInteraction']), duration ('90 days','180 days','1 year','10 years'), priority (int 1-5, unique).
- AUDIT_COPILOT_EVENTS: EXACTLY 10. Keys: time (ISO datetime June 2026), user (fictional email at northwindops.example), operation ('CopilotInteraction'), workload ('SecurityCopilot','Word','Teams','Outlook'), detail (one sentence: what was prompted/accessed, fictional).
- module.exports: { AUDIT_RETENTION_POLICIES, AUDIT_COPILOT_EVENTS }

Write the file now, then stop.
