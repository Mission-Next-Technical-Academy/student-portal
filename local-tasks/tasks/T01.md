# T01 - Security Copilot session list fixtures

You are generating fictional demo data for a local SC-200 training lab.

RULES (violating any = task failure):
- Create exactly ONE file: local-tasks/out/t01-copilot-sessions.js
- Do NOT read or edit any other file. Do NOT run git or install anything.
- All data is FICTIONAL demo data for a local training lab.
- No real URLs: the characters "http" must not appear anywhere in the file.
- No real companies/people. Fictional analyst names like 'R. Vance', 'M. Okafor'; fictional org names like 'Northwind Ops', 'Fabrikam-style is NOT allowed either — invent your own'.
- Any hash/ID-like strings use repeated-letter style: 'aaaa1111bbbb'.
- The file must be plain JavaScript and pass node --check.
- End the file with exactly:
if (typeof module !== 'undefined') { module.exports = { COPILOT_SESSIONS }; }

FILE CONTENT SPEC:
- Declare: const COPILOT_SESSIONS = [ ... ]; with EXACTLY 8 objects.
- Keys per object: id ('cs-001'..'cs-008'), name (short SOC investigation title), owner (fictional analyst), workspace ('Primary' or 'SOC-EU'), lastActivity (ISO datetime in June 2026, e.g. '2026-06-28T14:02:00Z'), promptCount (integer 2-14), plugins (array of 1-3 strings from: 'Defender XDR','Sentinel','Entra','Intune','MDTI'), pinned (true/false).
- Example first object:
  { id:'cs-001', name:'Phishing wave triage - finance dept', owner:'R. Vance', workspace:'Primary', lastActivity:'2026-06-28T14:02:00Z', promptCount:9, plugins:['Defender XDR','MDTI'], pinned:true },
- Vary the investigation themes: phishing, ransomware precursor, risky sign-in, OAuth app abuse, DLP escalation, vulnerability prioritization, TI enrichment, incident summary for management.

Write the file now, then stop.
