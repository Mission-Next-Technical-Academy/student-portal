# T06 - Defender Vulnerability Management fixtures

You are generating fictional demo data for a local SC-200 training lab.

RULES (violating any = task failure):
- Create exactly ONE file: local-tasks/out/t06-tvm.js
- Do NOT read or edit any other file. Do NOT run git or install anything.
- All data is FICTIONAL demo data for a local training lab.
- No real URLs: the characters "http" must not appear anywhere in the file.
- No real companies/people. Fictional analyst names like 'R. Vance', 'M. Okafor'; fictional org names like 'Northwind Ops', 'Fabrikam-style is NOT allowed either — invent your own'.
- Any hash/ID-like strings use repeated-letter style: 'aaaa1111bbbb'.
- The file must be plain JavaScript and pass node --check.
- End the file with exactly:
if (typeof module !== 'undefined') { module.exports = { TVM_SOFTWARE, TVM_CVES, TVM_RECOMMENDATIONS }; }

FILE CONTENT SPEC:
- Declare THREE consts, all fictional:
- TVM_SOFTWARE: EXACTLY 10. Keys: id ('sw-01'..), name (generic software like 'ArchiveMate','PDF Kiln' - fictional names, NOT real products), vendor (fictional), version, weaknesses (int 0-14), exposedDevices (int 0-40), threatInsight ('Exploit available','Active alert','None').
- TVM_CVES: EXACTLY 12. Keys: id ('cv-01'..), cve (FAKE pattern 'CVE-2026-90xx' range 9001-9012 so they cannot collide with real CVEs), severity ('Critical','High','Medium','Low'), cvss (number 3.0-9.9), software (one of the TVM_SOFTWARE names), exploitAvailable (bool), exposedDevices (int 0-40).
- TVM_RECOMMENDATIONS: EXACTLY 8. Keys: id ('tr-01'..), title ('Update <software> to version x.y'), software, exposedDevices, impact (number 1.0-9.0 exposure score points), status ('Active','Exception','Completed').
- module.exports: { TVM_SOFTWARE, TVM_CVES, TVM_RECOMMENDATIONS }

Write the file now, then stop.
