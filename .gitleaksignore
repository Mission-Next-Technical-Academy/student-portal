# Supabase publishable/anon key in portal/supabase-config.js — not a secret.
# Supabase's anon/publishable keys are designed to be public client-side;
# Row Level Security (see supabase/migrations/*_rls.sql) is the actual
# security boundary. The service-role key is never committed anywhere in
# this repo (see the comment above the flagged line).
portal/supabase-config.js:generic-api-key:13

# Imported Mission Next docs (portal/imported-labs/mission-next-labs/BUILD_PLAN.md) —
# a code-sample object key `observed.404Count` in a fictional test-fixture
# snippet, not a credential. Third-party doc content, not authored here.
portal/imported-labs/mission-next-labs/BUILD_PLAN.md:generic-api-key:80
