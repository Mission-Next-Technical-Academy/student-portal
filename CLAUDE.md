# Read this first

**Before doing anything else this session, read `NEXT_SESSION.md`.** It has an
urgent handoff block at the top (as of 2026-08-28) with unfinished work from
the previous session — an admin-only redirect that was promised but not yet
coded, plus two curriculum-alignment sprints that are spec'd but not started.

`CURRICULUM_ALIGNMENT_ARCHITECTURE.md` section 0 has the authoritative sprint
status table if `NEXT_SESSION.md` is ever out of date relative to it.
